// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider  = "prisma-client-js"
  url       = "postgres://postgres.dwmizhbjfjsuctzmtsti:birpz79dwNt0ueQu@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
  directUrl = "postgres://postgres.dwmizhbjfjsuctzmtsti:birpz79dwNt0ueQu@aws-0-us-east-1.pooler.supabase.com:6543/postgres"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Category {
  id       String    @id @default(uuid())
  name     String
  slug     String
  imageURL String
  products Product[]
}

model Product {
  id                 String   @id @default(uuid())
  name               String
  slug               String
  description        String
  basePrice          Decimal  @db.Decimal(8, 2)
  price              Float
  imageURL           String
  categoryId         String
  Category           Category @relation(fields: [categoryId], references: [id])
  discountPercentage Int      @default(0)
}
